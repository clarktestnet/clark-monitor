[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python monitor.py"
```
5. **"Commit changes"** dabao ✅

---

### Step 4 — Teesri file: `runtime.txt`

1. **"Add file"** → **"Create new file"**
2. File name:
```
runtime.txt
```
3. Content:
```
python-3.11.0
